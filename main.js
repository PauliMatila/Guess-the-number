function pickANumber(max)
{
    return Math.floor(Math.random()*max)
}

var secretNumber = pickANumber(100);
var quessedNumbers = 0;

function quessButtonPressed()
{
    var quessed = parseInt(document.quarySelector("#quessField").value);
}

function isValidQuess(number)
{
    if 
}